import type { ReportOptions, ReportOutputs } from '../types.js';

export function createReports(_options: ReportOptions): ReportOutputs {
  // TODO(TaskCard: Reports (Phase E1)) Emit JSON and CSV dry-run reports.
  void _options;
  throw new Error('createReports is not implemented in the skeleton.');
}